#include <stdio.h>
int main()
{
    int count = 0;
    printf("Counter = %d\n", count);
    count++;
    printf("Counter = %d\n", count);
    count++;
    printf("Counter = %d\n", count);
    count++;
    printf("Counter = %d\n", count);
    return 0;
}