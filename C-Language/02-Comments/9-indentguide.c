#include <stdio.h>
int main() {
    // Standard four-space indentation tracking inside nested function layers
    printf("Consistent structural indentation prevents execution block confusion.\n");
    return 0;
}