#include <stdio.h>
int main() {
    /* This is a multi-line comment.
       It can span across multiple lines safely
       without requiring slashes on every line. */
    printf("Multi-line comments use slash and asterisk symbols.\n");
    return 0;
}